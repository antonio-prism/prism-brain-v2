"""PRISM Brain Database ModelsSQLAlchemy ORM models for PostgreSQL.Updated with Phase 4B-4E columns for signals, explainability, and dependencies.Column names aligned with main.py code expectations."""from sqlalchemy import (    Column, String, Float, Integer, DateTime, Text, Boolean, JSON)from sqlalchemy.ext.declarative import declarative_basefrom datetime import datetimeBase = declarative_base()class RiskEvent(Base):    """Risk event with 905 events across multiple categories."""    __tablename__ = "risk_events"    id = Column(Integer, primary_key=True, autoincrement=True)    event_id = Column(String(50), unique=True, nullable=False, index=True)    event_name = Column(String(500), nullable=False)    layer1_primary = Column(String(100))    layer1_secondary = Column(String(100))    layer2_primary = Column(String(100))    layer2_secondary = Column(String(100))    baseline_probability = Column(Float, default=0.5)    baseline_1_5 = Column(Float, default=3.0)    super_risk = Column(Boolean, default=False)    baseline_impact = Column(Float)    methodology_tier = Column(String(50))    geographic_scope = Column(String(100))    time_horizon = Column(String(100))    description = Column(Text)    created_at = Column(DateTime, default=datetime.utcnow) 
