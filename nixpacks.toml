# Railway build configuration
[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python scripts/railway_init.py && uvicorn api.main:app --host 0.0.0.0 --port ${PORT:-8000}"
